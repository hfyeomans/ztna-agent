# =============================================================================
# ZTNA Agent — Ansible Inventory
# =============================================================================
# Update ansible_host with your server's IP (Terraform output or Elastic IP).
# Use ansible_ssh_private_key_file for the SSH key path on your machine.
# =============================================================================

[ztna_servers]
intermediate ansible_host=3.128.36.92 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/hfymba.aws.pem

[ztna_servers:vars]
ansible_python_interpreter=/usr/bin/python3
