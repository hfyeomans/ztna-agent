{
  "_comment": "Reference config for the macOS/iOS Agent. These values are passed via NETunnelProviderProtocol.providerConfiguration from the app UI, not read from a file.",
  "serverHost": "0.0.0.0",
  "_comment_serverHost": "Set via environment or config file — must be configured before use",
  "serverPort": 4433,
  "serviceId": "echo-service",
  "services": [
    {"id": "echo-service", "virtualIp": "10.100.0.1"},
    {"id": "web-app", "virtualIp": "10.100.0.2"}
  ]
}
